spring.application.name=proyectoTAW

# Puerto del servidor (opcional)
server.port=8080

# Configuraci�n de la base de datos MySQL (ajusta esto)
spring.datasource.url=jdbc:mysql://localhost:3306/taw?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=taw
spring.datasource.password=taw
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuraci�n de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuraci�n de vistas JSP
 spring.mvc.view.prefix=/WEB-INF/view/
spring.mvc.view.suffix=.jsp